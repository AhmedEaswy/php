<?php

    $file = '/dir2/dir1/myfile.php';
    /*
    // return file name
    echo basename($file);

    // return file name without extention
    echo basename($file, '.php');

    // return the dir name from path;

    echo dirname($file);

    */
    fopen('file.txt', 'text');